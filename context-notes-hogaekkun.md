# 호객꾼 "히로" 설치 — 컨텍스트 노트 (2026-08-22)

작업 중 내린 결정과 이유. 다음 세션은 이 파일부터 읽을 것.

## 사용자 확정 사항 (이 대화에서)
- **"나중에요" 버튼 폐지** — 원칙 자체를 업데이트(SKILL.md 3곳 수정). 억제는 "같은 화면 2분 쿨다운" 하나만. 손님이 캐릭터를 끌 스위치는 UI에 없다.
- **캐릭터** — 기존 브루탈리즘 SVG 마스코트를 승격, 이름 "히로". 새 이미지 생성 안 함(de-AI 방침·디자인 통일성).
- **전환 목표** — 연락처 수집이 아니라 "서비스·변호사에 대한 신뢰를 쌓아 퇴사 업무·괴롭힘 신고를 맡기고 싶게" 만드는 것. 연락처는 필수 단계.
- **톤 변경 동의** — 캐릭터가 전면, 김창희 변호사는 "최종 검토·직접 상담" 포지션. (변협 규정상 "자동 응답은 변호사가 사후 검토" 고지는 유지)
- **접수 후 대화 전문 문자 보고(신규 원칙)** — 연락처 접수된 대화는 전문을 빠짐없이 정리해 LMS로 보고. SKILL.md에 정립 완료.

## 설계 결정과 이유
- **ChatModal을 버리지 않고 승격** — 연락처 게이트·통보문 초안 퍼널·카카오 로그인·Firestore 로깅·손배협박 감지가 이미 검증돼 있어, 위젯을 새로 짜지 않고 "도크(HiroChat)가 ChatModal을 여는" 구조로 이식했다. 스토킹119의 JianChat 단일 컴포넌트 구조와 다른 지점.
- **전역 마운트** — 기존엔 Home에만 ChatModal이 있어 /calc·/diagnose 등에선 채팅이 아예 없었다. HiroChat(+ChatModal)을 App.tsx RootLayout에 마운트해 전 화면 커버. admin·delegation·print 화면은 도크 숨김.
- **openChat 연결** — Home의 컴포넌트들은 기존 `openChat` prop 유지, 구현만 `window.dispatchEvent(new CustomEvent("open-chat"))`로 교체. Calculator가 이미 쓰던 이벤트와 동일 채널.
- **대화창 열린 상태의 화면 안내(도달 경로 ③)** — HiroChat이 `hiro-page-intro` CustomEvent를 쏘고 ChatModal이 수신해 대화에 주입. 상태 끌어올리기 없이 결합 최소화.
- **표정↔마스코트 포즈 매핑** — 공감→empathy(신규 포즈), 결단→fly, 안심→wink, 응원→wave, 긴급→shock, 기본→stand. idle 영상 대신 CSS 눈 깜빡임(SVG 눈 그룹 scaleY 애니메이션).
- **엔진** — 기존 직접 fetch(claude-sonnet-5) 유지, @anthropic-ai/sdk 미도입(의존성 최소). Workers AI 폴백은 바인딩이 없어 생략 — 장애 시 정적 폴백 문구. 시스템 프롬프트에 cache_control(ephemeral) 적용.
- **API 응답 필드는 `text` 유지** — 열려 있는 옛 탭이 구버전 번들로 호출해도 깨지지 않게. expression/urgent만 추가.
- **블로그 지식 주입** — functions에서 src/generated/posts.ts를 직접 import하면 firebase.ts(DOM 의존)가 functions tsconfig(lib ES2022)로 타입체크돼 깨진다. prerender-posts.mjs가 functions/api/_blog-knowledge.ts(자급자족 모듈, slug·title·excerpt만)를 추가 생성하는 방식으로 해결.
- **대화 전문 보고 설계** — 트리거 ①모달 닫힘(fetch keepalive) ②pagehide(sendBeacon) ③초안 접수. 클라이언트가 sessionStorage에 세션별 "마지막 보고 메시지 수"를 기억, 새 내용 있을 때만 발송하고 매번 누적 전문을 보낸다(변호사는 최신 한 통만 읽으면 됨). 서버(notify.ts `chatlog`)는 1,800바이트 단위로 (1/n) 분할해 최대 6통 — 초과분은 어드민 안내로 대체. chatlog 전용 레이트리밋 버킷(기존 5회/10분과 분리)이라 상담 알림과 경합하지 않는다.
- **연락처 게이트 유지** — 익명 채팅 차단 방침(2026-08-19 지시) 그대로. 말풍선 화면 안내는 캐릭터의 일방 발화라 게이트와 무관.
- **긴급 프로토콜(노동 도메인)** — 자살·자해 암시는 109(자살예방, 24시간), 노동 문의는 1350(고용노동부), 사무소 1660-4452. 서버 정규식이 모델과 별개로 감지해 긴급 표정 강제 + 클라이언트 긴급 카드.

## 접수 누락 방지 A·B (2026-08-22 추가 — 사용자 "상담 접수 누락은 안된다" 지시)
- **A. 접수 이중화** — `saveConsultationDetailed()`가 핵심: 문자 알림을 DB 저장 성공 여부와 독립적으로 발송(이전엔 addDoc 성공 시에만 notify → Firestore 장애 = 전체 유실이었음). DB 실패 시 문자 머리에 "[주의] DB 저장 실패 — 이 문자가 유일한 기록" 표기. notifyNewConsultation은 boolean 반환 + 네트워크 1회 재시도. 연락처 제출은 두 경로 모두 실패 시에만 실패 안내(재시도·전화 1660-4452)를 띄우고 게이트를 유지한다 — 전달 안 된 연락처로 대화를 열면 그게 곧 누락이기 때문. saveDraftConsultation도 동일 구조로 재편.
- **B. 연락처 알림 한도 분리** — notify.ts에서 contact(4자 이상) 있는 요청은 전용 버킷(20회/10분, 사실상 무제한이되 봇 폭주만 차단). 일반 한도(5회/10분)와 경합 없음.
- 남은 C(솔라피 잔액·발송 실패 감시 초소)·D(미응대 재알림)는 lead-inbox Worker 쪽 별건 작업.
- 검증 중 프로덕션 Firestore에 "[테스트] 접수누락점검-무시하세요" 상담 레코드 1건 생성됨(어드민에서 무시·삭제 가능).

## C·D + 실시간 채팅 유실방지 (2026-08-22 추가 구현)
- **C·D는 lead-inbox Worker에 크론으로 구현** (`C:\Users\jeonw\.antigravity\lead-inbox`). `*/10 * * * *` = 미응대 재알림(D), `0 0 * * *`(=09:00 KST) = 솔라피 감시(C). 수동 실행 `POST /api/cron-run {job}` + 관리자 토큰(.tokens.txt).
- C 경보는 **문자+접수함 이중 채널** — 솔라피 인증이 죽으면(7월 시크릿 회전 사고 유형) 문자 자체가 안 나가므로, 접수함 목록에 '시스템경보' 행을 남긴다. 잔액 기준 5,000원 미만.
- ~~D 미응대 재알림~~ — **같은 날 도입→폐지**(사용자: "재알림 하지마 귀찮고 헷깔려"). 크론·코드·수동 잡 전부 제거, reminded_at 컬럼만 D1에 잔존(미사용). **재도입 제안 금지.**
- **ALERT_TO_PHONE은 운영자 번호로 시크릿 설정** — 값은 저장소에 기록하지 않는다. 변경하려면 Wrangler의 `secret put ALERT_TO_PHONE`을 사용한다(lead-inbox 디렉터리에서).
- 실시간 채팅 유실방지: ①`/api/chat-log`(서비스계정 REST, via:"server-fallback" 필드) — logChatMessage가 클라이언트 Firestore 실패 시 자동 폴백 ②대화 소강 3분 보고 + visibilitychange(hidden) 트리거. 폴백 레이트리밋 60회/10분.
- **대화 보고 원칙 정렬 완료**: 별도 전달 동의가 있는 경우 전문은 lead-inbox에 저장하고, 문자는 접수 사실만 한 통 발송한다. 동의가 없으면 대화 내용을 사무소 저장소·알림 경로로 보내지 않는다.
- **lead-inbox 관리자 화면 접근**: 주소 뒤에 `?token=...`을 붙여야 한다. 실제 토큰 값은 저장소나 문서에 기록하지 않는다.

## 연락처 전화번호 통일 + 성함 수집 (2026-08-22 사용자 확정)
- 게이트를 성함+전화번호 2칸으로 변경. 전화번호는 01X 숫자 10~11자리 형식 검증 후 하이픈 정규화, 카톡 ID·이메일은 히로가 정중히 거절하고 전화번호를 요청(프롬프트 [전환 연결]에 명시). 성함은 접수 payload(userName)·문자·접수함·대화록 보고·초안 접수·엔진 호칭([의뢰인 성함] volatile note)까지 전부 흐른다 — 접수함의 "미입력" 문제 해결.
- 검증: 브라우저 4분기(성함 누락/카톡ID 거절/정상 제출 포맷/완료 표시) + 프로덕션 실호출(카톡 ID 요청 → 전화번호 안내 응답 확인). 테스트 중 프로덕션 Firestore에 "홍길동/010-0000-1111" 테스트 상담 1건 생성됨(무시·삭제).
- 엔진 확인: 채팅은 `claude-sonnet-5`(chat.ts 216행, 2026-08-20 업그레이드 유지). 통보문 초안 생성기(draft.ts)는 `claude-haiku-4-5` — 별도 판단 필요 시 사용자 결정.

## "채팅이 안 넘어간다" 실사고 2건 수정 + 홈 섹션 안내 (2026-08-22 저녁)
- **게이트 무반응 버그**: 게이트 안내가 마지막 메시지인 상태에서 재전송하면 중복 방지 로직 때문에 화면에 아무 변화가 없었다 → 사용자가 "채팅이 안 넘어간다"로 인지. 수정: 차단 시마다 입력칸 흔들기(nudge 애니메이션)+빈 칸 포커스.
- **새로고침 재차단 버그**: 대화(hiro:msgs)는 복원되는데 연락처 제출 상태는 React 상태뿐이라 초기화 → 접수한 손님이 다시 차단. 수정: hiro:contact 키로 성함·번호·제출 상태를 세션에 보존.
- **홈 섹션 스크롤 안내**: 퇴사히어로 홈은 긴 한 페이지라 라우트 기반 안내만으론 히로가 조용했다(지안·독고실장과의 체감 차이의 실체). IntersectionObserver(화면 가운데 띠 기준)로 labor·contract-check·calc·process·lawyer·pricing 6개 섹션에서 먼저 말을 건다. 쿨다운 키 "/#섹션id", 2분.
- 검증: E2E 7/7(섹션 안내·게이트 반응·전송 라운드트립·새로고침 유지) + 실도메인 새 브라우저 스크린샷으로 선제 말풍선 확증. 테스트 흔적: 상담 "테스트검증/010-0000-2222" 1건.
- 사용자 화면에서 안 보였던 1차 원인은 옛 탭(배포 전 번들) — 완전 새로고침 필요.

## 동행 패널 전환 (2026-08-22 밤 — 사용자 "가운데 모달은 메인 같다, 지안처럼 우하단으로")
- ChatModal을 가운데 모달(백드롭+포커스 트랩)에서 **우하단 고정 패널(.hiro-panel, 396px, 백드롭 없음)**로 전환. 열린 채 사이트 스크롤·이동 가능.
- **핵심 발견**: 사이트 메뉴가 일반 `<a>`(전체 페이지 로드)라 이동 순간 패널이 닫혔다 → 열림 상태를 sessionStorage(hiro:open)에 보존, 열린 채 이동하면 등장 연출 없이 새 화면 안내를 대화에 자동 이어붙임(닫으면 말풍선 모드로 복귀, 자동 재열림 없음). E2E로 /faq 이동 후 패널 유지+안내 이어짐 확인.
- 서비스 설명 보강: 퀵칩에 "어떤 서비스예요?"·"비용이 궁금해요" 추가(+폴백 응답).
- 로컬 dev 함정: 죽인 wrangler의 workerd가 포트를 물고 좀비로 남아 이후 요청이 전부 타임아웃(000) — `Get-Process workerd | Stop-Process` 후 새 포트로.

## 법적 고지·동의 + 대화 보고 방향 교정 (2026-08-24)
감사에서 드러난 공백을 1~4순위로 처리. 상세 감사 결과는 이 절 아래 "남은 리스크" 참고.

- **개인정보처리방침 개정**(public/privacy.html, 5/4 → 8/24). 제1조 수집표에 "상담 챗봇 대화" 행 추가, **제1조의2(상담 챗봇 "히로" 대화의 처리)** 신설 — AI 처리 사업자 경유, 전달은 별도 동의 사항이며 기본 해제, 전달 방법, 민감정보 입력 금지 권고, 열람·삭제권. 제5조 위탁표에 **솔라피·Anthropic** 추가(+ 자체 접수함은 위탁 아님 명시).
- **동의 체크칸 2종**(ChatModal) — ①필수: 성함·연락처 수집·이용 동의(미체크 시 접수 전송 차단, 방침 링크 인라인) ②선택: 대화 내용 전달(**기본 해제**, 미체크 시 대화는 어디에도 안 나감). `shareChat`이 reportChatLog의 게이트이고 sessionStorage에도 보존(단, `=== true`일 때만 복원 — 저장값 손상 시 "동의함"으로 복원되지 않게).
- **대화 보고 방향 역전**(notify.ts handleChatLog) — 전문은 **중앙 접수함**에 저장(detail 앞머리 "[히로 대화 전문 · 전달 동의함]"), **문자는 간단 알림 한 통**. 두 채널 중 하나만 성공해도 ok(가짜 성공 금지의 이면). 일반 접수 경로도 `ok: r.ok || inboxOk`로 교정, detail slice 1500 → 12000, source 필드 추가.
- **대화 속 전화번호 캡처** — 서버 `PHONE_IN_TEXT` + `phoneDetected` 응답, 클라 `extractPhone`. **가장 중요한 지점은 게이트에 막히는 순간**(그 메시지는 서버에 안 닿으므로 여기서 안 잡으면 영영 유실) — 막힌 메시지에서 번호를 뽑아 접수칸에 프리필+안내. 접수 후 다른 번호가 나오면 "[연락처 변경]" 접수로 별도 기록.
- **어드민 채팅 로그 세션 묶음**(ChatLogs.tsx 전면 교체) — sessionId별 대화 카드, 펼치면 시간순 전체 대화. 기존 평면 테이블은 여러 손님이 뒤섞여 사실상 못 쓰는 화면이었다.
- **⚠ 별건 실사고 발견·수정 — 개인정보처리방침이 프로덕션에서 열리지 않았다.** `public/_redirects`의 `/privacy /privacy.html 200` 재작성이 Pages의 .html 자동 트리밍(308)과 맞물려 **무한 리다이렉트**. 규칙 2줄(terms 포함) 제거로 해결(Pages가 자동 서빙). **되살리지 말 것.**
- 검증: 로컬 E2E 8/8(동의칸 기본 해제·방침 링크·번호 캡처·필수 동의 차단·미동의 접수) + 프로덕션 chatlog 실호출(inboxOk·smsOk true, 접수함에 전문 저장·한글 정상) + /privacy·/terms 200 + 방침 문구 실도메인 확인.
- 테스트 흔적: 접수함에 "[테스트]동의검증 010-0000-3333", "[테스트]한글확인 010-0000-4444" 2건, 로컬 테스트로 Firestore에 "검증테스트 010-2345-6789" 1건. 무시·삭제 가능.
- ⚠ 한글 검증은 반드시 Python UTF-8로 — curl/PowerShell 경유는 깨져 보여 오판한다(실제로 이번에도 한 번 오판할 뻔).

## 남은 리스크 / 다음 세션 참고
- 프리뷰 환경엔 ANTHROPIC_API_KEY 시크릿이 없을 수 있다(프로덕션만 확인됨). 검증은 로컬 wrangler pages dev(.env의 키 사용) + 프로덕션 스모크로 진행.
- 한글 테스트는 반드시 Python UTF-8로(PowerShell CP949 함정 — 기존 메모리 참고).
- 배포는 git push가 아니라 wrangler 직접 업로드(project toesahero).


---

## Cloudflare에서 Anthropic 403 — Workers AI 폴백 (2026-08-28)

**증상.** 챗봇이 계속 "지금 대화 연결이 잠깐 원활하지 않네요"만 답했다.
어제 실제 손님(권고사직 상담)이 가장 중요한 말을 꺼낸 직후 이 문구를 받고 이탈했다.

**원인.** Anthropic이 `{"error":{"type":"forbidden","message":"Request not allowed"}}` 403을 돌려준다.
- 같은 키로 **로컬(집 IP)에서는 200**, **Cloudflare에서 나가면 403**
- 키를 hakjum 것으로 바꿔도 동일 → **키 문제 아님**
- hakjum이 멀쩡해 보였던 건 이미 `engine:"cf"`(Workers AI)로 폴백 중이었기 때문.
  즉 **두 사이트 모두 Anthropic이 막혀 있었다.**
- 크레딧 소진이면 400 `credit balance is too low`가 오므로, 이건 키 비활성화·조직 차단 쪽.
  → **Anthropic 콘솔에서 IP 허용목록·지역 제한·워크스페이스 설정 확인 필요(미해결)**

**조치.** hakjum의 엔진 전환 구조를 이식했다. Claude가 던지면 Workers AI로 넘어가 대화를 잇는다.
- Pages에 AI 바인딩 추가(`ai_bindings: {AI:{}}`) — 원래 없었다
- 모델 `@cf/openai/gpt-oss-120b`
- **폴백 모드에서는 법률 안내를 금지**한다(`CF_FALLBACK_NOTE`). 공감 → 상황 질문 → 연락처 유도까지만.
  품질이 Claude보다 낮은 엔진이 법리를 말하면 안 하느니만 못하다.
- 응답에 `engine` 필드를 실어 어느 엔진이 답했는지 운영에서 보이게 했다

**함정 둘 (실제로 겪음).**
1. **gpt-oss는 output에 reasoning과 message를 함께 싣는다.** 앞에서부터 첫 text를 집으면
   `"We need to respond as per rules..."` 같은 영어 사고문이 그대로 손님에게 나간다.
   → `type === "message"`인 항목만 취할 것. sanitize에도 2차 필터를 뒀다.
2. **사고 과정이 max_output_tokens를 함께 먹는다.** 500이면 답변이 중간에 잘린다.
   → 1500 + `reasoning:{effort:'low'}`. 응답도 13초 → 2~4초로 빨라졌다.

**남은 일.** 콘솔에서 403 원인이 풀리면 Claude로 되돌아간다(코드 수정 불필요, 자동).
근본 대안은 미국 리전(Firebase Functions us-central1) 경유 — 품질 저하 없이 우회 가능.
