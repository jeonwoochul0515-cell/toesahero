# 퇴사히어로 예행연습 체크리스트 (2026-09-12)

페르소나 — 실제 접수 두 건.
- **윤창우** 임금체불(주휴·공휴일·월차), 5인 이상, 서류 보유. 광고로 새벽 3시 유입.
  "계약서에 무서운 조항이 있어요"를 두 번 말했으나 챗봇이 회수 못 하고 끊김.
- **yuri.m** 입사 3주, 불안증·공황으로 퇴사 의사를 못 꺼냄. 카카오 채널 유입. 기본 절차 199,000원.

사무실 — 실무 전우철 / 담당변호사 김창희.

## 1차 발견 (코드 추적, 2026-09-12)

### 당장 막히는 것
- [x] ① 결제 후 「내 사건 보기」가 빈 화면 — 로그인 없이 결제하면 사건이 계정에 안 붙고, 나중에 로그인해도 안 따라옴
- [x] ② 위임장 서명 화면으로 가는 길이 손님 화면에 없음
- [ ] ③ 위임장 제출이 결제·접수 건과 연결되지 않음 (사람이 손으로 맞춤)
- [x] ④ 올린 자료를 본인이 못 지움 — 저장소는 삭제 가능, 목록은 불가라 반쪽 삭제 위험
- [x] ⑤ 결제 실패로 돌아와도 안내가 없음

### 매일 쓰다 보면 답답한 것
- [ ] ⑥ 챗봇이 되묻고 나서 정형 안내로 대화를 덮음 (호객꾼 스킬 §6-1 정책은 세웠으나 코드 미반영)
- [ ] ⑦ 중앙 접수함(lead-inbox)에 같은 세션이 여러 건으로 쌓임
- [x] ⑧ 진행 상황을 보려면 로그인이 필요한데 결제 전에 안 알려줌

### 말이 어렵거나 불안하게 하는 것
- [x] ⑨ 손님 화면에 "결제 인프라 미설정" 같은 개발 용어
- [x] ⑩ 본인확인 실패 시 영어 오류 코드를 손님이 옮겨 적게 함
- [ ] ⑪ 접수 경로 이름에 상품명·서류명이 섞임("베이직: 통보문")

## 고칠 순서
1. 결제·접수를 전화번호로 계정에 잇기 (①⑧)
2. 결제 실패 안내 (⑤)
3. 위임장 가는 길 + 사건 연결 (②③)
4. 올린 자료 본인 삭제 (④)
5. 챗봇 질문 직후 정형 안내 억제 (⑥)
6. 중앙 접수함 세션 단위 갱신 (⑦)
7. 손님 화면 말 다듬기 (⑨⑩⑪)

## 2차 발견 (백그라운드 점검 8개 · 총 149건)

영역별 원본은 `docs/ux-audit/01~08`. 아래는 **코드로 다시 확인한 것만** 옮겼다.

### 확인 · 이번에 고침
- [x] **발송 API에 인증이 전혀 없었다** — 외부인이 사무소 명의로 메일 발송 가능 (03-1). `_admin-auth.ts` 신설
- [x] **내 사건 조회 실패가 "없습니다"와 같게 보임** + consultations 복합 색인 누락 (02-1)
- [x] **결제 503 때 "직접 입금" 안내 → 이중 지불 위험** (01-1)
- [x] **서버 반영 실패해도 손님에겐 성공만 표시** (01-3) — 늦어질 수 있음을 알림

### 확인 · 2차 수정에서 고침 (커밋 `82b7a52`)
- [x] **같은 사건 중복 결제를 아무도 막지 않음** (01-2) — 결제된 사건에는 결제 화면 대신 "이미 결제가 완료되었습니다"
- [x] **결제해도 진행 단계가 「신규 접수」에 멈춤** (02-2) — 보여 주는 단계만 보정. 저장값은 변호사가 관리
- [x] **자해·자살 신호가 사무실에 전달되지 않음** (04-1) — `notify.ts`에 safety 경로 신설. 동의·연락처와 무관하게 사무실에 닿고, 대화 내용은 보내지 않는다
- [x] **대화창에 번호를 적으면 개인정보 동의 검사를 건너뜀** (04-2) — 동의 전에는 저장하지 않고 번호를 접수칸에 옮겨 둔다
- [x] **승인한 글과 실제 발송되는 글이 다를 수 있음** (03-2) — 저장본과 다르면 발송을 막는다
- [x] **위임장 서명이 가로로 늘어나 저장·인쇄됨** (03 덧붙임) — `aspect-ratio`로 좌표계(600x220)에 맞춤
- [x] **관리자 저장 실패가 화면에 표시되지 않음** (06-3)
- [x] **실업급여 계산기가 아무 값 없이도 792만원을 내놓음** (05-1) — 입력 전에는 계산하지 않고, 가입기간이 짧으면 요건 미충족 가능성을 알린다(단정하지 않음)
- [x] **FAQ의 「카톡으로 문의」가 홈으로만 감** (07-1)
- [x] **광고 랜딩 5종에 전화 버튼이 없음** (07-3)
- [x] **챗봇이 "무료" · "당연한 권리" 등 규정선을 넘는 말을 함** (08-3) — 문구 교체 완료
- [x] 노무사 비교표·"사직서 양식 무료 다운로드" 표기 (07 덧붙임) — **2026-09-12 대표 전우철 판단으로 둘 다 유지. 고치지 않는다.**

### 확인 · 3차 수정에서 고침 (2026-09-13)
- [x] **오래된 사건은 상세가 영원히 "로드 중"** (06-1) — 문서 하나만 직접 구독하도록 바꾸고
      "이 사건을 찾을 수 없습니다"로 끝맺는다. 인쇄 화면도 같이 고쳤다
- [x] **채팅 사건의 상태·메모가 손님이 말을 더 걸면 사라짐** (06-2) — 같은 대화의 상태·메모를
      대표 문서에 승계한다. 실무자가 「신규」로 되돌린 것은 옛 값이 덮지 않는다(필드 존재 여부로 판정).
      회귀 방지 테스트 6개 추가(`src/admin/groupChatSessions.test.ts`)
- [x] **계산기 접수가 AI 초안 생성에 묶여 있고 끊길 때 문자 우회가 없음** (05-3=문서 05-6) —
      초안 호출에 20초 타임아웃, DB 실패 시 사무실 문자가 유일한 기록으로 나간다.
      오프라인에서 저장이 무기한 대기해 문자까지 막던 것도 8초에서 끊는다(독립 검토 지적)
- [x] **홈이 약속한 "초안 받아보기"를 주지 않음** (08-2) — "담당변호사 김창희가 통보문을
      작성해 드립니다"로 바꾸고 "체험"·"자동 생성" 표현을 뺐다

### 기각 · 사실과 다름
- (없음 — 이번 회차에서 확인한 주장은 모두 코드와 일치했다)

### 미확인 (코드로 다시 보지 않음)
- 나머지 100여 건. 심각도 「답답」·「말」 계열이 대부분이다.

## 검증

- `npx tsc -b` exit 0 · `npm test` 14개 통과 · `npm run build` exit 0 (수정 3회 모두)
- 커밋 `c263604` `b11fafa` `5a923f3` `82b7a52`

### 배포 확인 (2026-09-13 재확인)

2026-09-12 수정분은 **전부 실서비스에 올라가 있다.** 위 "아직 배포하지 않았다"는 그날
오전 시점의 기록이었고, 같은 날 배포가 끝났다. 2026-09-13에 아래로 다시 확인했다.

| 무엇 | 확인 방법 | 결과 |
|---|---|---|
| 화면 코드 | 실서비스 번들과 로컬 `dist` 의 md5 비교 (`app` · `ChatModal` · `CheckoutPage`) | 전부 일치 |
| Cloudflare Pages | `latest_deployment.environment` | `production`, 2026-09-12 06:22 KST |
| Firestore 보안 규칙 | Rules API 로 받은 실서버 규칙과 `firestore.rules` diff | 동일 (게시 06:07 KST) |
| Firestore 색인 | `firebase firestore:indexes` | `consultations(uid, createdAt)` 포함, 일치 |

## 3차 수정 검증 (2026-09-13)

- `npx tsc -b` exit 0 · `npm test` **20개** 통과(14 → 20, groupChatSessions 6개 추가) · `npm run build` exit 0
- 커밋 `c99e5ec` `6992b93` `a14a769`
- 독립 검토(codex) 2회 — HIGH 1건(오프라인 addDoc이 문자 발송까지 막음),
  MEDIUM 1건(정렬 없는 limit) 모두 ACCEPT 후 수정·재검증
- **배포 완료** — 2026-09-13 09:54 KST, Cloudflare Pages `environment == production`, branch `main`.
  실서비스 번들 md5 대조로 반영 확인(`app` · `ConsultationDetail` · `PrintLetter` 전부 일치),
  홈에 "통보문 작성 요청하기" 노출, IndexNow 통지 HTTP 200.
  (첫 배포 시도는 PC 메모리 부족으로 빌드가 죽었고, 재시도해서 성공했다.)

## 남은 것 (1차 발견 중)

- [ ] ③ 위임장 제출이 결제·접수 건과 연결되지 않음 (사람이 손으로 맞춤)
- [ ] ⑥ 챗봇이 되묻고 나서 정형 안내로 대화를 덮음 (호객꾼 스킬 §6-1 정책은 세웠으나 코드 미반영)
- [ ] ⑦ 중앙 접수함(lead-inbox)에 같은 세션이 여러 건으로 쌓임
- [ ] ⑪ 접수 경로 이름에 상품명·서류명이 섞임("베이직: 통보문")

## 코드로 다시 보지 않은 것

2차 점검 149건 중 100여 건. 심각도 「답답」·「말」 계열이 대부분이다.
관리자 화면 쪽에는 아직 안 본 지적이 더 있다 — 합계 숫자가 최근 N건만 센다(06-1 원문 1),
상태 변경 이력이 안 남는다(06-4), 목록에 전화번호가 없다(06-6), 비밀번호 재설정이 없다(06-8).

## 개선 지시서(2026-09-25) — 10개 시나리오 점검 결과

원문: 대표가 준 `toesahero_improvement_prompt.md`. 대표 결정(2026-09-25):
결제 방식(A 바로 결제 / B 상담 후 결제)은 **보류 — 현재·변경 후 비교표를 보고 정함**.
셀프 진단은 **연락처를 받은 뒤 결과를 보여주는 지금 방식 유지**(상담 확보 우선, 지시서 2-5 기각).
홈 12→6 섹션 축소(3단계)는 1·2단계 뒤 따로 정함.

### 1단계 — 결정 없이 고칠 것
- [ ] 1-1 푸터 "GA4 바로가기" 링크 제거
- [ ] 1-2 1000px 이하 햄버거 메뉴 + 헤더 신청 버튼
- [ ] 1-3 390px 가로 스크롤(요금 셀프진단 버튼, 비교표)
- [ ] 1-4 /calc 입력 범위 강제·필수값/전화번호 오류 표시·근속 1년 미만 퇴직금 안내
- [ ] 1-5 /calc "퇴사 사유" 셀렉트 → /unemployment-calc 링크로
- [ ] 1-6 /unemployment-calc 사유 문구 분기·수급 곤란 시 총액 흐림·나이 범위·0 입력
- [ ] 1-7 개인정보 동의 공통 컴포넌트 → /calc·/unemployment-calc·/diagnose·/checkout
- [ ] 1-8 /my 같은 사건 중복 생성

### 2단계 — 결제 방식 결정 뒤
- [ ] PAYMENT_FLOW 상수, 요금 카드 버튼 순서, 패키지 선택기, 히로 결제 안내, /calc·/my 결제 버튼, FAQ 결제 문항, /checkout 제목
- [ ] 휴대폰 하단 고정바 — 히로 버튼과 겹치지 않게 배치

### 4단계 — 법률 문구 (고친 뒤 변호사 확인 목록으로 보고)
- [ ] 4-1~4-12 (지시서 원문 번호 그대로)
