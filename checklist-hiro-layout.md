# 히로 대화창 레이아웃 개편 체크리스트 (2026-09-06)

문제: 대화가 화면의 14%(117px)만 차지하고 나머지 86%를 고정 UI가 먹는다.
모바일(733px)에서는 대화에 29px만 남아 사실상 대화가 보이지 않는다.
게다가 「연락처 남기기」 버튼이 가로로 잘려 화면 밖으로 나가 있어 **누를 수 없다**(기능 고장).

목표: 대화 영역을 화면의 절반 이상으로 되돌리고, 연락처는 "나중에, 겹쳐서" 묻는다.

## 확정된 방향 (사용자 승인 2026-09-06 "전부개편")
- [x] 연락처를 첫 마디 직후가 아니라 **대화가 쌓인 뒤** 묻는다
- [x] 폼이 대화 공간을 **영구히 뺏지 않게** 겹쳐 뜨는 시트로 만든다
- [x] 연락처 게이트 자체는 유지 (2026-08-19 방침) — 묻는 시점과 방식만 바꾼다
- [x] 변호사법 §23 고지문은 그대로 유지 (법적 필수)

## 구현
- [x] 1. `.hiro-stage` 래퍼 추가 — 대화 영역을 position:relative 무대로
- [x] 2. `.contact-sheet` 신규 — 대화 위에 겹쳐 올라오는 연락처 시트 (닫기 가능)
- [x] 3. `.contact-strip` 신규 — 평소 노출되는 한 줄 띠 "연락처 남기고 답변 이어받기"
- [x] 4. `contactSheetOpen` state + 자동 펼침 트리거 3개
      - [x] 사용자 3번째 메시지 이후 (1회만)
      - [x] 통보문 초안 「검토 요청」 시
      - [x] 창 닫기 시도 시 (1회만, 두 번째는 그냥 닫힘)
- [x] 5. 동의 문구 축약 — (선택) 항목을 `<details>`로 접어 5줄→2줄
- [x] 6. `.chat-contact` 세로 배치 + `flex-wrap` — 버튼 잘림 수정
- [x] 7. `.quick-replies` 대화 시작 후 가로 스크롤 1줄 (143px→48px)
- [x] 8. 하단 여백 정리 — 카톡 버튼·고지문 패딩 축소

## 검증
- [x] `npm test` 통과
- [x] `npm run build` 통과 (tsc -b + functions 타입체크 포함)
- [x] 396px 폭에서 대화 영역 440px 이상 확보
- [x] 360px 폭 / 733px 높이(모바일)에서 대화 350px 이상 확보
- [x] 좁은 폭에서 「연락처 남기기」 버튼이 잘리지 않고 보이는지
- [x] 시트를 닫으면 대화가 그대로 복귀하는지
- [x] 연락처 제출 후 시트·띠가 모두 사라지고 완료 문구만 남는지
- [x] 새로고침 후 이미 접수한 사람에게 시트가 다시 뜨지 않는지 (sessionStorage 복원)

## 건드리지 않은 것
- 접수 이중화(`saveConsultationDetailed`)·대화 전문 보고·긴급 프로토콜 — 그대로
- 대화 중 전화번호 자동 감지 접수(`send` 안의 `extractPhone`) — 그대로
- CSS `.chat-contact.nudge` 는 tsx에서 쓰이지 않는 죽은 코드다(이번 작업과 무관, 삭제하지 않음)

## 실측 결과 (2026-09-06, vite preview + 실제 브라우저)
| | 개편 전 | 개편 후 |
|---|---:|---:|
| 데스크톱(860px 패널) 대화 — 초안 CTA 있음 | 117px | 284px |
| 데스크톱 대화 — 초안 CTA 없음 | 117px | 431px |
| 모바일(733px 패널) 대화 — 초안 CTA 있음 | 29px | 178px |
| 모바일 대화 — 초안 CTA 없음 | 29px | 304px |
| 빠른질문 | 143px(3줄) | 62px(1줄 가로스크롤) |
| 연락처 영역 상시 점유 | 246px | 52px(띠) |
| 「연락처 남기기」 버튼 | **잘려서 안 보임** | 전체 폭 360px, 정상 |

시트를 여닫아도 대화 높이 284px 그대로 유지(띠가 `visibility:hidden`으로 자리를 지킨다).
