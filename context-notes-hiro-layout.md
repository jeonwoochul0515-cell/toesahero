# 히로 대화창 레이아웃 개편 — 컨텍스트 노트 (2026-09-06)

작업 중 내린 결정과 이유. 다음 세션은 이 파일부터 읽을 것.

## 왜 손댔나 — 측정값
사용자 제보로 캡처(폭 396px 패널, 1.5배 스케일)를 실측했다. 실제 픽셀 환산.

| 영역 | 높이 | 비중 |
|---|---:|---:|
| 헤더 + 본인확인 줄 | 120px | 15% |
| **대화 내용** | **117px** | **14%** |
| 빠른질문 6개(3줄) | 143px | 17% |
| 연락처 안내 문구 | 53px | 6% |
| 동의 체크박스 2개 | 113px | 14% |
| 연락처 입력 줄 | 80px | 10% |
| 직접 입력 + 보내기 | 73px | 9% |
| 카카오톡 버튼 | 60px | 7% |
| 하단 고지문 | 62px | 8% |

대화 14%, 껍데기 86%. 채팅창이 아니라 "대화창이 붙은 신청서"였다.
모바일(dvh 745 → 패널 733px)에서는 고정 영역 704px가 그대로 필요해 **대화에 29px만 남는다.**

## 발견한 기능 고장 (가독성보다 심각)
`.chat-contact`가 `display:flex`인데 `flex-wrap`이 없고(styles.css:1737), 부모 `.modal`이
`overflow:hidden`(styles.css:439)이다. 자식 셋(성함 84px + 전화번호 + 버튼 nowrap)이 폭을 넘겨
**「연락처 남기기」 버튼이 잘려 화면 밖으로 나가 있었다.** 성함·전화번호를 다 입력해도 누를 버튼이 없다.
Enter 키로는 제출되지만(ChatModal.tsx:911) 폰 키보드에서 Enter를 누르는 사람은 거의 없다.
⇒ 이 상태로 얼마나 운영됐는지 모른다. 접수 누락이 있었을 가능성이 있으니 어드민 유입 추이를 확인할 것.

## 설계 결정과 이유

### 1. "나중에 묻기"만으로는 부족하다 — 시점 + 배치를 같이 바꾼다
기존은 사용자가 첫 마디를 던지는 순간(`hasStartedConversation`, ChatModal.tsx:762) 폼이 뜨고
**그 뒤로 영원히 안 사라졌다.** 시점만 미루면 문제가 뒤로 이동할 뿐 — 뜨는 순간 똑같이 절반을 먹는다.
그래서 ①시점을 미루고 ②겹쳐 띄운다를 함께 적용했다.

### 2. 시트(겹침) vs 아코디언(밀어내기)
아코디언은 열릴 때 대화를 아래로 밀어내 스크롤 위치가 튄다. 시트는 대화 위에 얹히므로
닫으면 대화가 픽셀 단위로 그대로 복귀한다. `.hiro-stage`(position:relative)를 새로 두고
그 안에 `.modal-body`와 `.contact-sheet`(absolute)를 넣는 구조.
입력줄·빠른질문은 무대 밖이라 시트가 떠도 계속 쓸 수 있다.

### 3. 자동 펼침 트리거 3개
- **사용자 3번째 메시지 이후** — 두세 마디 주고받아 "내 상황을 아는구나" 싶어진 뒤가 수락률이 높다. 1회만.
- **통보문 초안 「검토 요청」** — 가장 자연스럽다. 초안을 받으려면 연락처가 필요하다는 이유가 스스로 설명된다.
  초안 *생성*은 연락처 없이도 되게 뒀다(무료 미끼). 게이트는 검토 요청 단계에 건다.
- **창 닫기 시도** — 마지막 기회. 단 1회만이고, 시트에 「그냥 닫기」를 명확히 둔다.
  (CLAUDE.md §12-1 — 빠져나갈 길 없는 막다른 안내를 만들지 않는다. 두 번째 닫기는 무조건 닫힌다.)

### 4. 동의 문구를 줄이되 법적 요건은 유지
(필수) 개인정보 수집·이용 동의는 한 줄로 유지. (선택) 대화 전달 동의는 한 줄 + `<details>`로 접었다.
전문은 펼치면 그대로 보이므로 고지 의무를 해치지 않는다. 5줄 → 2줄.

### 5. 빠른질문은 없애지 않고 1줄로
대화 중에도 "비용이 궁금해요"는 유용하다. 지우는 대신 대화 시작 후 가로 스크롤 1줄로 접었다(143→48px).
대화 시작 전에는 지금처럼 6개를 다 펼친다 — 첫 화면에서는 이게 대화를 여는 손잡이다.

### 6. 고지문은 손대지 않는다
"변호사 비밀유지 의무 · 자동 응답은 김창희 변호사 사후 검토 · 변호사법 제23조 광고물"은
변협 규정상 필수 표시다(전역 CLAUDE.md §21, §23). 문구는 그대로 두고 폰트·패딩만 줄였다.

## 예상 효과
| | 개편 전 | 개편 후 |
|---|---:|---:|
| 데스크톱 패널(822px) 대화 영역 | 117px (14%) | 약 440px (54%) |
| 모바일(733px) 대화 영역 | 29px (4%) | 약 350px (48%) |


---

# 괴롭힘 상품 통일 (2026-09-06, 같은 세션 별건)

## 무엇이 문제였나
괴롭힘을 다루는 상품이 둘이었다.
- `Pricing.tsx` laborTiers — 「직장 내 괴롭힘 대응」 **66만원** (결제 경로 없음, 상담 신청만)
- `Pricing.tsx` tiers.max — 「분쟁 대응」 **79만원** (결제 가능), 항목에 "직장 내 괴롭힘 신고 자문" 포함

79만원이 66만원을 통째로 삼키는 모양이라 손님 입장에서 66만원을 살 이유가 없었다.
정작 진짜 차이인 "퇴사하느냐 / 재직하며 다투느냐"는 카드 어디에도 적혀 있지 않았다.

## 사용자 결정 (2026-09-06)
**79만원 「분쟁 대응」 하나로 통일.** 결제되는 상품으로 모은다.

## 바꾼 것
- `Pricing.tsx` — laborTiers에서 `harassment-response`(66만원) 카드 삭제. 참조하는 코드는 없었다.
- `SegmentLandingPage.tsx:53` — /harassment 랜딩 가격을 "분쟁 대응 · 79만원 (정액)"으로 수정
- `Pricing.tsx` — 노동분쟁 섹션에 안내 한 줄 + `/harassment` 링크 추가.
  섹션 설명에 "괴롭힘을 겪고 있다면"이 있는데 카드를 지우면 그대로 막다른 안내가 되기 때문이다(CLAUDE.md §12-1).
- `styles.css` — `.labor-harassment-note`

## 건드리지 않은 것과 이유
- **JSON-LD `serviceType`의 "직장 내 괴롭힘 대응"** — 상품 카드가 아니라 제공 서비스 목록이다. 괴롭힘 대응은 계속 하므로 유지(SEO에도 필요).
- **`/harassment` 랜딩 페이지 자체** — 콘텐츠·SEO 자산이라 그대로 둔다. 가격만 79만원으로 맞췄다.
- **`functions/api/chat.ts`** — 챗봇은 이미 "분쟁 대응(790,000원 — 괴롭힘·부당해고·산재·손해배상)"으로 안내하고 있었다. 즉 **화면이 챗봇과 어긋나 있던 것**이고, 이번 수정으로 양쪽이 맞았다.
- **부당해고 66만원 상품** — 79만원 「분쟁 대응」의 "해고"와 똑같은 겹침이 남아 있다. 이번 지시는 괴롭힘 한정이라 손대지 않았다. **다음에 정리할 후보.**

## 남은 것
- 66만원 부당해고 vs 79만원 분쟁 대응 겹침 (위 참조)
- laborTiers 상품(88만·66만)은 여전히 결제 경로가 없다 — 상담 신청으로만 이어진다


---

# 통보문 초안 CTA — 변협 광고규정 대응 (2026-09-06)

## 문제
손님이 「통보문 초안 생성하기」를 누르면 AI가 만든 통보문 **전문이 손님 화면에 그대로** 떴다(`draft-preview`).
전역 CLAUDE.md §23이 정리한 금지 양태 중 아래에 닿는다.

| 양태 | 해당 여부 |
|---|---|
| ② 소비자가 AI 프로그램을 **직접 사용하게 하는** 방식 | **정면으로 해당** — 버튼을 눌러 결과물을 직접 받았다 |
| ① AI를 업무에 이용한다는 사실을 **광고** | "자동 생성해드릴 수 있습니다" 표현 |
| ⑤ 검토 변호사 **성명 미표시** | CTA에 "변호사"만 있고 김창희 이름이 없었다 |

⚠ **규정 원문은 확인하지 못했다.** 협회 자치규정이라 법제처 API에 없고 변협 회원 로그인이 필요하다.
2차 자료 기준 판단이며 **최종 확인은 김창희 변호사** 몫이다.

## 사용자 결정 (2026-09-06)
**구조 전환.** 문구만 고치면 ②가 그대로 남으므로 결과물을 받는 사람을 손님에서 변호사로 바꾼다.

## 바꾼 것
- `requestDraft` + `submitDraftForReview` → **`requestLetter` 하나로 통합**
- 초안은 `/api/draft`로 생성해 **접수함(`saveDraftConsultation`)에만 저장**하고 손님에게는 보이지 않는다
- 손님에게는 "요청이 접수되었습니다 (접수번호) — 김창희 변호사가 작성·검토해 연락드립니다"만
- CTA 문구 — "자동 생성해드릴 수 있습니다" → "**김창희 변호사가 직접 작성**해 보내드립니다"
- 버튼 — "통보문 초안 생성하기" → "**변호사에게 통보문 작성 요청**"
- 연락처 게이트를 **요청 시작 시점**으로 이동. 회신처가 없으면 API를 부르지 않고 시트를 연다(실측 확인).
- 고아가 된 `draftLetter` state와 `.draft-preview*`·`.draft-tag` CSS 제거

## 다음 세션이 지킬 것
**손님 화면에 초안 전문을 다시 노출하지 말 것.** 되돌리면 그대로 ②에 걸린다.
`ChatModal.tsx`의 `requestLetter` 위에 같은 취지의 주석을 달아 두었다.

## 남은 것
- 변협 규정 원문 확보 후 김창희 변호사 확인 (지금 판단은 2차 자료 기준)
- 이 사이트 말고도 호객꾼이 설치된 곳(부산회생프로·law-caddy·학폭119·이별119·면허지킴·바른증거)에
  같은 구조가 있는지 점검 — 전역 CLAUDE.md §23의 적용 대상이다
