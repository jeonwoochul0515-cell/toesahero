# site-qa 점검 노트 — 퇴사히어로 (2026-09-11)

실행 `node ~/.claude/skills/site-qa/scripts/site-qa.mjs --config site-qa.config.json`
설정 `site-qa.config.json` (신규 작성) · 보고서 `qa-report.json`

## 결과

| | 처음 | 지금 |
|---|---:|---:|
| REQUIRED FAIL | 6 | **1** |
| WARN | 8 | 9 |
| PASS | 49 | 60 |
| NOT_RUN | 1 | 0 |

## 고친 것 — 진짜 결함

| 항목 | 내용 | 조치 |
|---|---|---|
| **H02** | `/diagnose`·`/foreign-workers`에 **변호사법 제23조 광고물 고지가 없었다.** 두 화면은 전체 Footer를 쓰지 않는다 | `src/App.tsx` 루트 푸터에 고지 추가 — 전 화면 공통이라 같은 사고가 재발하지 않는다 |
| **D03** | 조직 entity 이름이 두 가지로 섞여 있었다. `법률사무소 청송law` 138회 / `퇴사히어로 (법률사무소 청송law)` 48회. **검색엔진이 한 조직으로 못 묶는다** | `index.html`·`public/privacy.html`·`public/terms.html`의 name을 `법률사무소 청송law`로 통일. `퇴사히어로`는 alternateName에 그대로 있어 잃는 정보가 없다 |
| **C03** | 칼럼 2편에 공유 이미지 파일이 없었다 | `npm run og`로 31장 재생성 |
| **G06 재발 방지** | 칼럼 자동 생성기 프롬프트가 **`법률사무소 청송`**(전역 §13 금지 표기)이라고 자기 사무소를 불렀고, 모델이 그것을 **`법무법인 청송`**으로 변주했다 | `scripts/auto-publish-daily.mjs` 프롬프트를 `법률사무소 청송law`로 고치고 **금지 표기 목록을 명시**했다 |

## 남은 REQUIRED FAIL 1개 — 대표님이 하셔야 한다

**G06 `/blog/auto-mttm11bg` 본문에 "법무법인 청송"이 실서비스에 나가 있다.**

- 해당 문장 — "… 확인하시길 권합니다. **법무법인 청송은** 의뢰인의 상황을 성실히 검토하여 …"
- 고칠 값 — `법무법인 청송` → `법률사무소 청송law`
- **글은 Firestore에 있고 관리자 로그인이 필요하다.** 로컬 `.env`에 `ADMIN_EMAIL`/`ADMIN_PASSWORD`가 없어(CI 전용) 여기서 고칠 수 없다.
- 고치는 곳 — https://toesahero.com/admin/blog 에서 「계약직 재계약 거절, 부당해고 아닌가요 — 갱신기대권」 글을 열어 본문 수정
- 발행 글 31편 중 **이 1곳뿐**이다(전수 확인). 생성기는 이미 고쳤으므로 새 글에는 재발하지 않는다.

## 측정 오류였던 것 — 기준을 낮춘 것이 아니다

| 항목 | 왜 틀린 판정이었나 | 조치 |
|---|---|---|
| **A07/A08/A09** 사이트맵 | 이 프로젝트는 `functions/sitemap.xml.ts`가 **요청 시점에** 만든다. dist에 파일이 없는 것이 정상이고 실서비스는 HTTP 200·URL 46개로 정상 제공된다 | 검사기에 `runtimeArtifacts` 설정을 추가해 **그 주소에서 받아 같은 검사를 그대로** 돌린다. 검사를 건너뛰지 않는다 |
| **H10** 개인정보처리방침 | config에 `/privacy.html`로 적었는데 검사기는 라우트 표기(`/privacy`)를 쓴다 | 설정 수정 |
| **G08** 주소 | 빈 문자열이 들어가 실행 오류(NOT_RUN)였다 | 실제 주소를 설정에 채움 |

## 사유를 적고 넘어가는 WARN

- **A04 학습 크롤러 미차단** — GPTBot·ClaudeBot·Applebot-Extended. **결함이 아니라 정책 결정**이다. 지금은 허용이 유리하다고 본다(AI 답변 인용 도달).
- **A05 전용 그룹의 `/__/` 미상속** — Firebase 내부 경로다. 노출돼도 해가 없어 급하지 않다.
- **G08 주소 45/47** — 칼럼의 "연제구"는 JSON-LD `PostalAddress`의 `addressLocality`다. **구조화 주소 표기가 맞다.** 검사기가 marker를 보면 전체 문자열을 요구하는데 구조화 주소에는 해당하지 않는다.
- **E01/E02 JS 1,484KB (firebase 615KB)** — 실제 개선 여지가 있다. 다만 이번 작업 범위가 아니다.
- **B16 `/my`·`/checkout`** — 비공개 경로라 산출물에 없는 것이 정상이다.
- **F02/F03 접근성** — 계산기 3개 화면의 입력 라벨, 홈의 main 랜드마크. 실제 개선 여지가 있다.

## ★ 원래 질문의 답 — 네이버에 "안 잡히는" 것이 아니다

네이버 검색 API로 직접 쟀다.

| 검색어 | 우리 자연 순위 |
|---|---|
| 퇴사히어로 | **1위** |
| 퇴사히어로 변호사 | **1위** |
| 법률사무소 청송law | 9위 |
| toesahero | 17위 |

**사이트는 정상 색인돼 있다. 기술 결함이 아니다.**

그런데 광고 키워드 598개 중 592개가 30위 밖인 이유는 **경쟁 문서 수**다.

| 검색어 | 네이버 웹문서 총 건수 |
|---|---:|
| 퇴사대행 | 902,386 |
| 퇴사통보 | 1,177,966 |
| 임금체불 | 4,374,419 |

**칼럼 31편으로 437만 건 안에서 30위에 드는 일은 일어나지 않는다.**
브랜드명(고유명사)에서 1위이고 일반 경쟁어에서 순위가 없는 것은 신생 사이트의 정상 상태다.

⇒ **할 일은 기술 수정이 아니라 두 가지다.**
1. **칼럼을 늘려 롱테일을 잡는다.** 경쟁 문서가 적은 상황 서술형 검색어부터.
   광고에서 산 자리(회사미지급수당·퇴사말하기무서워 등)와 같은 말을 제목으로 쓴다.
2. **네이버 생태계를 함께 쓴다.** 통합검색 상단은 블로그·카페·플레이스가 차지한다.
   외부 사이트는 그 아래라, 네이버 블로그와의 원소스 멀티유즈(OSMU)가 필요하다.
   (전역 검색 가이드 §10·§18.5)

## 다음에 다시 돌릴 때

```
npm run build
node ~/.claude/skills/site-qa/scripts/site-qa.mjs --config site-qa.config.json
node ~/.claude/skills/site-qa/scripts/site-qa.mjs --config site-qa.config.json --only G06
```
