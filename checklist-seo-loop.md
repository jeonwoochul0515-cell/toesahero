# 검색 최적화(SEO·AEO·GEO) 루프 — 퇴사히어로 (2026-09-06)

채점기: `scripts/seo-score.mjs` (네이버광고 스킬 §5). 실행 `node scripts/seo-score.mjs`

## 점수 변화
| 바퀴 | 점수 | 한 일 |
|---|---:|---|
| 최초 | 79.61 | 채점기 이식 |
| 1 | 75.48 | 칼럼 경로를 `/blog/`로 고침 → **정직한 하락**(전엔 0편이라 아예 안 봤다) |
| 2 | 82.66 | 측정 오류 3건 수정(낫표 표기·날짜 형식·사이트맵 런타임) |
| 3 | 85.23 | 칼럼 면책 문구, 한 문장 정의 |
| 4 | 86.89 | 정책 페이지 2개에 메타·공통 head |
| 5 | 90.05 | RSS·llms 판정, 두괄식 요약(lead-box) |
| 6 | **90.20** | 랜드마크(header/footer) 보강 |

## 고친 것 — 실제로 좋아지는 것들 (스킬 §5-6)
- [x] 칼럼 두괄식 요약 상자 — 글 전체를 읽기 전에 답부터 준다
- [x] **본문 법령 조문 → 법제처 원문 링크 자동 변환**(`src/lib/lawLinks.ts`, 19/29편)
      손님은 근거를 확인하고, 답변엔진은 출처가 달린 문서로 인용한다
- [x] 칼럼 면책 문구(구체적 사건의 자문이 아님) 29편 전부
- [x] 한 문장 정의 전 화면 노출 — 검색·답변엔진이 사이트를 하나의 대상으로 인식
- [x] WebSite·Person 구조화 데이터 (변호사 김창희를 독립 엔티티로)
- [x] RSS 자동발견 링크, manifest, 보안 헤더(HSTS·nosniff 등)
- [x] JS 번들 분할 — 가장 큰 묶음 1030KB → 615KB
- [x] `<main>`/`<header>`/`<footer>` 랜드마크
- [x] 정책 페이지(privacy·terms) 메타·구조화·랜드마크

## 측정 오류로 판정을 고친 것 (근거는 코드 주석에 남김)
원본 채점기는 이별119/스토킹119 기준이라 이 프로젝트와 어긋나는 판정이 있었다.
- 칼럼 경로 `/columns/` → **`/blog/`**
- sitemap·rss·llms를 **Cloudflare Functions가 런타임 생성** — dist에 파일이 없다고 0점을 주면
  "만들지 않았다"로 오판한다. 라이브는 전부 200이고 각각 URL 44개·item 29개·칼럼 29개를 담는다.
- 광고물 고지 `/변호사법 제23조/` → **낫표 허용**(「변호사법」 제23조가 정식 표기)
- 갱신일 `20\d\d\.\d+\.\d+` → **"2026년 8월 3일" 형식도 인정**
- 비공개 화면: 하드코딩된 `['my-space','live','admin','calc']` → **robots.txt Disallow로 판정**.
  이 프로젝트에서 `calc`는 광고 랜딩으로 쓰는 **공개** 화면이고 admin·my·checkout은 SPA 라우트다.

## 남은 것 — 칼럼 29편의 내용을 실제로 다시 써야 하는 작업
점수를 위해 내용을 지어내지 않는다(스킬 §5-5). 아래는 글을 실제로 고쳐야 오른다.
- [ ] **칼럼 H2의 절반 이상을 질문형으로** (현재 3%, 4점 항목 — 가장 큰 구멍)
- [ ] 칼럼 제목을 질문형으로 (28%) — ⚠ 제목 변경은 기존 색인·외부 링크에 영향. 신중히
- [ ] 칼럼당 FAQ 3개 이상 (0%) + FAQPage 구조화 데이터
- [ ] 목록·표로 절차 제시 (10%) — "지금 하실 일" 번호 목록
- [ ] 칼럼 본문 깊이 (평균 1819자 → 3000자)
- [ ] 칼럼마다 고유 og:image (3%) — 슬러그별 이미지 생성 필요.
      ⚠ SVG로 만들면 채점은 통과하나 카톡·페북 미리보기가 안 뜬다. PNG로 만들 것
- [ ] JS 총량 1378KB (900KB 목표)

이 항목들을 다 하면 98점대에 도달한다. 다만 **글을 실제로 개선하는 작업**이라 별도 세션이 맞다.
